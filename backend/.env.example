# Database Configuration
# PostgreSQL with PostGIS:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/animal_surveillance

# Fallback SQLite for local testing without Docker:
# DATABASE_URL=sqlite:///./animal_surveillance.db

HOST=0.0.0.0
PORT=8000
